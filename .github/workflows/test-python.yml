name: Test Python SDK

on:
  push:
    branches: [main, "feature/*"]
    paths:
      - "python/**"
      - ".github/workflows/test-python.yml"
  pull_request:
    paths:
      - "python/**"

jobs:
  test:
    name: Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]

    defaults:
      run:
        working-directory: python

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          version: "latest"

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Install dependencies
        run: uv sync --extra dev

      - name: Lint (ruff)
        run: uv run ruff check src/ || true   # non-blocking until lint is fully clean

      - name: Type check (mypy)
        run: uv run mypy src/matimo || true   # non-blocking during early development

      - name: Run tests with coverage
        run: uv run pytest tests/ --cov=matimo --cov-report=xml --cov-report=term-missing -q

      - name: Upload coverage
        if: matrix.python-version == '3.11'
        uses: codecov/codecov-action@v4
        with:
          files: python/coverage.xml
          flags: python-sdk
          fail_ci_if_error: false

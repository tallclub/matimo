name: composio_outlook_get_master_categories
description: >-
  Tool to retrieve the user's master category list. use when you need to get all categories defined
  for the user.
version: 1.0.0
risk: low
parameters:
  composio_user_id:
    type: string
    description: The Composio entity/user ID for the calling tenant or user.
    required: true
  composio_connected_account_id:
    type: string
    description: >-
      The Composio connected account ID for the OUTLOOK toolkit, scoped to the calling tenant. The
      "Connect OUTLOOK" flow must be completed before this tool will succeed.
    required: true
  top:
    type: number
    description: Maximum number of master categories to return (OData $top).
    default: null
  skip:
    type: number
    description: Number of master categories to skip for pagination (OData $skip).
    default: null
  filter:
    type: string
    description: OData $filter query to filter master categories.
    default: null
  select:
    type: array
    description: OData $select query to specify properties to include.
    default: null
  orderby:
    type: array
    description: OData $orderby query to order master categories by property values.
    default: null
  user_id:
    type: string
    description: The user's unique identifier or principal name. Use 'me' for the signed-in user.
    default: me
execution:
  type: http
  method: POST
  url: https://backend.composio.dev/api/v3/tools/execute/OUTLOOK_GET_MASTER_CATEGORIES
  headers:
    x-api-key: '{COMPOSIO_API_KEY}'
    Content-Type: application/json
  body:
    user_id: '{composio_user_id}'
    connected_account_id: '{composio_connected_account_id}'
    arguments:
      _matimo_tool: composio_outlook_get_master_categories
      top: '{top}'
      skip: '{skip}'
      filter: '{filter}'
      select: '{select}'
      orderby: '{orderby}'
      user_id: '{user_id}'
  timeout: 30000
authentication:
  type: api_key
  location: header
  name: x-api-key
output_schema:
  type: object
  description: Response envelope from Composio's tool execution endpoint.
  properties:
    success:
      type: boolean
      description: Whether the HTTP request to Composio succeeded (2xx status).
    data:
      type: object
      description: Composio's response body.
      properties:
        data:
          type: object
          description: The action-specific result payload.
        error:
          type:
            - string
            - 'null'
          description: Error message if Composio could not execute the action.
        successful:
          type: boolean
          description: Whether Composio successfully executed the underlying action.
tags:
  - composio
  - outlook
notes:
  env: COMPOSIO_API_KEY
  toolkit: outlook
  composio_slug: OUTLOOK_GET_MASTER_CATEGORIES

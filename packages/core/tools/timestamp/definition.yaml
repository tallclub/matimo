name: timestamp
description: Returns the current ISO timestamp
version: "1.0.0"

parameters: {}

execution:
  type: command
  command: node timestamp.ts

output_schema:
  type: object
  properties:
    timestamp:
      type: string